{
  "serviceDetails": {
    "envSpecificDetails": {
      "buildCommand": "npm install && npm run build -w apps/api",
      "startCommand": "npm run db:migrate -w apps/api && npm run db:seed -w apps/api && npm run start -w apps/api"
    }
  }
}
